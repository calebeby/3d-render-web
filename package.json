{
  "name": "canvas-polygon-renderer",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npm run build-rust && tsc && vite build",
    "build-rust": "cd rust && wasm-pack build --target web --dev",
    "preview": "vite preview"
  },
  "devDependencies": {
    "prettier": "^2.6.0",
    "typescript": "^4.6.2",
    "vite": "^2.8.6"
  },
  "prettier": {
    "trailingComma": "all"
  }
}
