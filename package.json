{
  "name": "canvas-polygon-renderer",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-rust": "wasm-pack build --target web --dev",
    "preview": "vite preview"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  },
  "prettier": {
    "trailingComma": "all"
  }
}
